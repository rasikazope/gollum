#include <stdio.h>
#include <list>
#include <queue>
#include <iostream>
#include <vector>
#include <stack>
#include <unordered_map>

using namespace std;


string
reverse_vowels(string mystr) 
{
    int i = 0, j = mystr.size();

    while (i <= j) {
        
    }

    return mystr;
}

int main() {

    cout << reverse_vowels("leetcode") << endl;
    return 0;
}
